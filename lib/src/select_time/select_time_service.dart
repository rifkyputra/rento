import 'package:rento/src/select_time/select_time_controller.dart';

import '../abstract/abstract.dart';

class SelectTimeService implements SaveSubmit {
  @override
  saveLocal(TimeDuration timeDuration) async {
    ///
  }

  @override
  submitData(TimeDuration timeDuration) async {
    ///
  }
}
